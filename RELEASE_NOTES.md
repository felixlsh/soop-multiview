# SOOP Multiview 0.3.0

Windows x64용 채팅·알림 강화 테스트 릴리스입니다. **코드 서명되지 않은 실행 파일**입니다.

## 달라진 점

- **선택 기록 보관:** 지정한 키워드·계정 채팅, 후원, 알림 발생 원문만 방송 입장부터 퇴장까지 로컬 임시 저장합니다. 일반 채팅 전체를 저장하지 않습니다.
- **채팅 필터:** 복수 방송 선택, 본문·닉네임·계정 ID 구분, ID 정확 일치, 포함·제외 단어, 강조 표시와 최대 8개 저장 필터를 지원합니다. 화면 검색과 저장 조건은 독립적입니다.
- **앱 알림함:** 최대 20개 키워드 규칙, 원문 열기, 읽음 처리, 묶음 보기와 이전 기록 조회를 지원합니다.
- **Windows 알림:** 설치형에서 선택적으로 켤 수 있습니다. 기본은 꺼짐이며 원문 미리보기·소리도 기본으로 꺼집니다. 같은 규칙·방송은 30초, 전체는 5초 간격으로 제한하고 나머지 일치는 앱 알림함에 남깁니다. 포터블은 앱 알림함을 사용합니다.
- **후원 보기:** 별풍선·영상풍선·애드벌룬·미션 종류, 보낸 계정, 최소 수량으로 필터링합니다. 방송별 수신 건수는 조회한 200개만이 아니라 현재 세션 전체 조건 일치 기록을 기준으로 계산합니다.

최대 4개 방송, 입장 시 공식 채팅 표시·스크린 모드·자동 수신, 새 방송 음소거 기본값은 유지합니다. 사용자가 수동으로 중지한 수신과 닫은 같은 방송을 자동 재개하지 않습니다.

## 저장과 업데이트

0.2 위에 설치하면 기존 설정·즐겨찾기를 유지합니다. 새 보관 조건과 알림 규칙은 빈 상태, Windows 알림은 꺼진 상태로 시작합니다. **채팅 기록 → 보관 조건**, **알림함 → 알림 규칙**에서 원하는 조건을 설정하세요.

설정·SOOP 세션과 선택 기록의 위치는 `%APPDATA%\SOOP Multiview`입니다. 선택 기록은 임시 SQLite 파일에 보관하고 방송 제거·로그아웃·정상 종료 시 삭제합니다. 강제 종료로 남은 파일은 다음 실행 때 정리하며 원문을 복원하지 않습니다. 재연결·새로고침·수신 중지는 현재 시청 세션의 기존 기록을 지우지 않습니다.

최근 30분·방송당 2,000개 기준의 자동 삭제를 제거했습니다. 조회는 200개씩 나누며 방송당 저장 한도 64 MiB와 쓰기 대기열 한도를 적용합니다. 한도·저장 오류가 생기면 보관 상태와 누락을 안내합니다. 운영체제에 전달된 Windows 알림은 Windows가 관리합니다.

- `Setup.exe`: 설치 마법사, 설치형 Windows 알림 사용 가능
- `Portable.exe`: 설치 없이 실행, 앱 알림함 사용. 설정은 USB가 아닌 현재 PC에 저장
- `SHA256SUMS.txt`: 다운로드 파일 무결성 확인용 해시

설치형과 포터블은 같은 PC의 설정을 공유합니다. 업데이트는 새 버전을 직접 다운로드해 적용합니다.

## 검증과 제한

단위 테스트 179개, 앱 통합 검사 76개, 최종 패키지 기능 검사 12개가 통과했습니다. 0.2 → 0.3 설치 업데이트에서 기존 설정 파일 유지, 설치본·배포본 ASAR의 SHA-256 일치, ASAR SQLite 워커와 정상 종료 후 임시 파일 정리를 확인했습니다. 실제 포터블 실행과 설치형 바로가기 미변경도 확인했습니다.

합성 선택 기록 12,000개와 20개 규칙이 적용된 알림 4,000개를 누락 없이 저장·조회했습니다. 후원 종류·수량·읽음 처리는 합성 입력으로 검증했으며 SOOP에 채팅·후원을 보내지 않았습니다.

공개 방송 4슬롯을 30분 관측했습니다. 3개에서 영상 진행과 자동 채팅 수신을 확인했고, 1개는 공식 플레이어 초기화 상태에 머물러 본방 재생을 확인하지 못했습니다. 24분 30초에 재생 중인 3개 영상의 일시정지를 관측했으며 모두 다음 30초 샘플에서 회복했습니다. 앱 오류·선택 기록 저장 누락은 0건이고 정상 종료 후 임시 채팅 파일은 남지 않았습니다. **4개 방송 모두의 30분 연속 재생은 미입증**입니다.

설치형에서 실제 Windows 테스트 알림의 `show` 이벤트와 바로가기 연결을 확인했습니다. 앱 내부 클릭 처리의 창 복원·방송 선택·원문 열기는 합성 이벤트로 통과했습니다. **실제 Windows 알림 표면 클릭은 조작 도구 초기화 오류로 미확인**입니다. 방해 금지 설정이나 Windows 정책에 따른 화면 표시를 보장하지 않으며, 앱 종료 후 알림과 실행 복원은 지원하지 않습니다.

실제 로그인 계정 즐겨찾기 검증은 보류 상태입니다. 관심 계정은 부분 확인 결과입니다. 후원 표시는 수신 이벤트이며 실제 결제·환불·미션 성공·정산 완료나 전체 방송의 후원 내역을 의미하지 않습니다. SOOP 내부 이벤트·플레이어 변경에 따라 호환 수정이 필요할 수 있습니다.

SOOP 공식 제품이 아니며 공식 플레이어·로그인·광고·구독과 시청 제한을 따릅니다.
